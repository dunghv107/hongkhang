# Nhà trọ Hồng Khang — Agent Guide

## 1. Bắt buộc đầu phiên

- Đọc toàn bộ file này trước khi phân tích, lập kế hoạch, sửa code hoặc chạy lệnh.
- Trả lời và viết nội dung giao diện bằng tiếng Việt tự nhiên, ngắn gọn, đúng ngữ cảnh người tìm trọ.
- Kiểm tra code hiện có trước khi tạo file, component, dependency hoặc abstraction mới.
- Không sửa, xóa hoặc ghi đè thay đổi của người dùng ngoài phạm vi yêu cầu.

## 2. Bối cảnh sản phẩm

- Tên thương hiệu: **Nhà trọ Hồng Khang**.
- Giai đoạn hiện tại: website công khai giúp khách xem phòng, giá, tiện ích, vị trí, tình trạng còn phòng và liên hệ thuê.
- Giai đoạn sau: mở rộng thành hệ thống quản lý nhà trọ. Không xây trước dashboard, phân quyền, hợp đồng, hóa đơn hoặc nghiệp vụ quản lý khi chưa được yêu cầu.
- Ưu tiên hiện tại: thông tin đáng tin cậy, ảnh phòng rõ ràng, trải nghiệm mobile tốt, liên hệ nhanh và SEO địa phương.

## 3. Hạ tầng và dữ liệu

- Supabase: cơ sở dữ liệu, xác thực và các chức năng backend phù hợp.
- Vercel: build, deploy và hosting ứng dụng; **không dùng để lưu ảnh tải lên**.
- Cloudinary: nguồn lưu trữ và phân phối ảnh phòng. Trong Supabase chỉ lưu URL/public ID cùng metadata cần thiết.
- Cloudflare: DNS cho domain; không tự ý thay đổi DNS, cache rule hoặc security rule nếu chưa có yêu cầu rõ ràng.
- Không đưa secret vào source. Chỉ expose biến bắt đầu bằng `NEXT_PUBLIC_` khi dữ liệu đó thực sự an toàn ở trình duyệt.
- Khi thay đổi schema Supabase, dùng migration có thể review; không sửa production thủ công.

## 4. Nguyên tắc triển khai

- Áp dụng `ponytail` mặc định: giải pháp nhỏ nhất thực sự hoạt động, ưu tiên native/stdlib và dependency đã có; không dựng kiến trúc cho nhu cầu tương lai.
- Với công việc UI, dùng theo thứ tự: `impeccable` để xác định chất lượng và quy trình, `ui-ux-pro-max` để kiểm tra UX/accessibility/responsive, rồi skill taste phù hợp để tạo cá tính thị giác.
- Skill hỗ trợ quyết định, không được ghi đè brief, bảng màu, accessibility, hiệu năng hoặc tính đúng đắn.
- Giữ component đơn nhiệm và tái sử dụng khi có nhu cầu thật. Không tạo wrapper/helper chỉ có một nơi dùng nếu code trực tiếp rõ hơn.
- Không thêm package nếu nền tảng hoặc dependency hiện tại đã giải quyết được.
- Mọi thay đổi phải được kiểm tra bằng lint/typecheck/test/build phù hợp đang có trong dự án.

## 5. Hệ thống thiết kế bắt buộc

Không rải mã hex trong component. Khai báo một lần bằng semantic design tokens và dùng token xuyên suốt:

| Token gợi ý | Màu gốc | Vai trò |
|---|---:|---|
| `--color-canvas` | `#f2f2f2` | nền trang |
| `--color-surface` | `#d3d2d1` | bề mặt phụ |
| `--color-muted` | `#a7a4a1` | viền, chữ phụ có kiểm tra tương phản |
| `--color-brand` | `#5d5248` | thương hiệu, CTA phụ |
| `--color-soft` | `#c4bfba` | nền nhấn nhẹ |
| `--color-ink` | `#000000` | chữ chính, CTA tương phản cao |

- Phong cách: ấm, mộc, sạch, đáng tin, mang cảm giác nơi ở thật; tránh giao diện SaaS chung chung và hiệu ứng phô trương.
- Thiết kế mobile-first; kiểm tra tối thiểu tại 375px, 768px, 1024px và màn hình desktop rộng.
- Body tối thiểu 16px trên mobile, line-height dễ đọc, vùng bấm tối thiểu 44×44px.
- Tương phản chữ thường tối thiểu 4.5:1. `#a7a4a1` không dùng cho chữ nhỏ trên nền sáng nếu chưa đạt tương phản.
- Mỗi màn hình có một CTA chính rõ ràng, ví dụ “Xem phòng còn trống” hoặc “Liên hệ xem phòng”.
- Dùng icon SVG từ một bộ nhất quán; không dùng emoji làm icon giao diện.
- Motion phải có mục đích, ưu tiên transform/opacity và hỗ trợ `prefers-reduced-motion`.

## 6. Nội dung và SEO

- Mỗi trang có title, meta description, canonical và Open Graph phù hợp; chỉ một `h1`, heading theo đúng thứ bậc.
- Trang phòng cần URL ổn định, nội dung riêng, giá và tình trạng rõ ràng, alt ảnh mô tả thực tế.
- Ưu tiên SEO địa phương: tên “Nhà trọ Hồng Khang”, khu vực/địa chỉ chính xác, thông tin liên hệ và dữ liệu có cấu trúc phù hợp khi có đủ dữ liệu thật.
- Không bịa đánh giá, số lượng phòng, tiện ích, địa chỉ, giá, khoảng cách hoặc cam kết.
- Sitemap, robots và structured data phải phản ánh đúng route/indexing thực tế; không tạo schema giả chỉ để tăng SEO.
- Ảnh Cloudinary phải có kích thước responsive, định dạng/tối ưu tự động phù hợp, khai báo kích thước để tránh CLS; ảnh ngoài màn hình đầu dùng lazy loading.

## 7. Quy tắc Git và phạm vi file

- Chỉ commit source, cấu hình mẫu, migration và tài liệu thuộc dự án.
- Không commit thư mục skill/agent sinh cục bộ, cache, secret, log, build output hoặc cấu hình cá nhân.
- Không force-push, reset, xóa branch hoặc chạy thao tác phá hủy nếu người dùng chưa yêu cầu rõ ràng.
- Trước khi bàn giao: xem diff, bảo đảm không có secret và nêu rõ các kiểm tra đã chạy.

## 8. Tiêu chí hoàn thành

- Đúng yêu cầu hiện tại, không xây tính năng tương lai ngoài phạm vi.
- Responsive, keyboard-accessible, focus rõ, ảnh có alt phù hợp và không có horizontal scroll.
- Không upload ảnh vào Vercel/Supabase Storage nếu kiến trúc đã chốt Cloudinary.
- Không có secret trong client hoặc Git.
- Lint/typecheck/test/build liên quan vượt qua, hoặc báo chính xác phần chưa thể chạy và lý do.
